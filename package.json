{
  "name": "livechat-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node -e \"const fs=require('node:fs'); for (const p of ['dist','release']) fs.rmSync(p,{ recursive:true, force:true });\"",
    "dev": "vite build --watch",
    "build": "vite build",
    "build:clean": "npm run clean && npm run build",
    "package": "python3 scripts/package.py",
    "release:build": "npm run build:clean && npm run package",
    "ci:check": "npm run typecheck && npm run lint && npm run test && npm run build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint \"src/**/*.{ts,tsx}\" \"vite.config.ts\" \"vitest.config.ts\""
  },
  "devDependencies": {
    "@types/chrome": "^0.0.289",
    "@typescript-eslint/eslint-plugin": "^8.15.0",
    "@typescript-eslint/parser": "^8.15.0",
    "eslint": "^10.0.0",
    "typescript": "^5.6.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  },
  "overrides": {
    "minimatch": "^10.2.3",
    "flatted": "^3.4.1",
    "rollup": "^4.59.0"
  }
}
